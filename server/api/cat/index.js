'use strict';

var express = require('express');
var controller = require('./cat.controller');

var router = express.Router();

//router.post('/', controller.createCat);
//router.get('/:id', controller.readCat);
//router.put('/:id', controller.editCat);
//router.delete('/:id', controller.removeCat);

module.exports = router;
